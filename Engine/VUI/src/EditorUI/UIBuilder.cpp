#include <EditorUI/UIBuilder.h>

// Called when we want to construct the UI from a UINode structure.
void UIBuilder::Construct(const UINode& InNode) {
    Expects(0);
}

// Called when we want to rebuild the UI from the app directly. 
void UIBuilder::HotReload() {
    Expects(0);
}
